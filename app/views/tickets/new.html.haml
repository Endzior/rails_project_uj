.page-header
  %h1 
    Creating a new ticket
    %small 
      as 
      = current_user.full_name
      to project
      = @project.name
= render 'form'
%br/
%a.btn.btn-default{:href => project_path(@project)}
  Back to 
  = @project.name